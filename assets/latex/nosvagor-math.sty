% ███╗   ███╗ █████╗ ████████╗██╗  ██╗
% ████╗ ████║██╔══██╗╚══██╔══╝██║  ██║
% ██╔████╔██║███████║   ██║   ███████║
% ██║╚██╔╝██║██╔══██║   ██║   ██╔══██║
% ██║ ╚═╝ ██║██║  ██║   ██║   ██║  ██║
% ╚═╝     ╚═╝╚═╝  ╚═╝   ╚═╝   ╚═╝  ╚═╝
%  →-.   →  .-'.   →  .--.   →  .--.   →  .--.   →  .-→
% →::.\::::::::.\::::::::.\::::::::.\::::::::.\:::::::→→ →
% → →:.\:::: https://github.com/nosvagor/notes \:::→→ →  →  →
% →::::.\::::::::.\::::::::.\::::::::.\::::::::.\::::→  →
%   → →   `--'  →   `.-'  →   `--'  →   `--'  →   `--' →  →

\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{nosvagor-math}

% ╔═╗┌─┐┬─┐┌┬┐┌─┐┌┬┐┌┬┐┬┌┐┌┌─┐
% ╠╣ │ │├┬┘│││├─┤ │  │ │││││ ┬
% ╚  └─┘┴└─┴ ┴┴ ┴ ┴  ┴ ┴┘└┘└─┘
\RequirePackage{
  mathtools, amssymb, bm, relsize, esvect,tikz,tikz-cd, array,marvosym,
  etoolbox, mathdots,
}

  \apptocmd{\lim}{\limits}{}{}

  \numberwithin{equation}{chapter}
  \DeclareMathSizes{12}{14}{10}{8}

  % Augmented Matrix [ccc|c]
  \makeatletter
      \renewcommand*\env@matrix[1][*\c@MaxMatrixCols c]{%
      \hskip -\arraycolsep
      \let\@ifnextchar\new@ifnextchar
      \array{#1}}
  \makeatother

  % bigger epsilon fonts
  \let\origVarEpsilon\varepsilon
  \renewcommand*\varepsilon{\mathlarger{\mathlarger{\mathlarger\origVarEpsilon}}}
  \def\dvarepsilon{\mathsmaller{\varepsilon}}

  \let\origEpsilon\epsilon
  \renewcommand*\epsilon{\mathlarger{\mathlarger{\mathlarger\origEpsilon}}}
  \def\depsilon{\mathsmaller{\epsilon}}

% ╔═╗┌─┐┌┬┐┌┬┐┌─┐┌┐┌┌┬┐┌─┐
% ║  │ │││││││├─┤│││ ││└─┐
% ╚═╝└─┘┴ ┴┴ ┴┴ ┴┘└┘─┴┘└─┘

% Inline bold math
\newcommand{\tbm}[1]{\(\bm{#1}\)}

% Alternative Zero Font
\newcommand{\nil}{\text{\MVZero}~}

% ihat, jhat, khat
\newcommand*{\ih}{
    \mbox{\fontsize{17.28}{21.6}\selectfont\(\hat{\imath}\)}}
\newcommand*{\jh}{
    \mbox{\fontsize{17.28}{21.6}\selectfont\(\hat{\jmath}\)}}
\newcommand*{\kh}{\hat{k}}

% adding with frequent use
\newcommand{\R}{\mathbb{R}}
\newcommand{\N}{\mathbb{N}}
\newcommand{\Z}{\mathbb{Z}}
\newcommand{\F}{\mathcal{F}}
\newcommand{\I}{\1{\mathcal{I}}}
\def\hopital{L'H\^{o}pital}

% Operator names
\def\med{\operatorname{med}{\hspace{-2pt}}}
\def\cov{\operatorname{cov}{\hspace{-2pt}}}
\def\sgn{\operatorname{sgn}{\hspace{-2pt}}}
\def\var{\operatorname{var}{\hspace{-2pt}}}
\DeclareMathOperator{\arcsec}{arcsec}
\DeclareMathOperator{\arccot}{arccot}
\DeclareMathOperator{\arccsc}{arccsc}
\newcommand{\rref}[1]{\operatorname{rref}\left(#1\right)}
\newcommand{\tr}[1]{\operatorname{tr}\left(#1\right)}
\newcommand{\rank}[1]{\operatorname{rank}\left(#1\right)}
\renewcommand{\det}[1]{\operatorname{det}\left(#1\right)}
\newcommand{\proj}[2]{\operatorname{proj}_{\bm{#1}}\bm{#2}}
\DeclarePairedDelimiterX{\norm}[1]{\lVert}{\rVert}{#1}

% Spacing in
\def\qand{\quad\text{and}\quad}

% Redfined with color
\let\oldcup\cup
\renewcommand{\cup}{\hspace{3pt}\FF{\oldcup}\hspace{2pt}}
\let\oldcap\cap
\renewcommand{\cap}{\hspace{3pt}\TT{\oldcap}\hspace{2pt}}

\let\oldmin\min
\renewcommand{\min}[1]{\BB{\oldmin\left(\fg{#1}\right)}}
\let\oldmax\max
\renewcommand{\max}[1]{\RR{\oldmax\left(\fg{#1}\right)}}

\let\oldland\land
\renewcommand{\land}{\,~\TT{\oldland}~}
\let\oldlor\lor
\renewcommand{\lor}{~\FF{\oldlor}~}

\RequirePackage{stackengine}
\renewcommand{\pm}{\mathbin{\ensurestackMath{\abovebaseline[-4pt]{%
  \stackunder[-4pt]{\color{red}\mathsmaller{+}}{\color{blue}-}}}}}

\def\then{~\2{\implies}~}
\def\thus{~\2{\therefore}~}

\let\oldiff\iff
\renewcommand{\iff}{~\2{\oldiff}~}

\def\lt{~\BB{<}~}
\def\gt{~\RR{>}~}

\def\conv{\text{\TT{converges}}}
\def\dive{\text{\FF{diverges}}}
