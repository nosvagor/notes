% ███╗   ███╗ █████╗ ████████╗██╗  ██╗
% ████╗ ████║██╔══██╗╚══██╔══╝██║  ██║
% ██╔████╔██║███████║   ██║   ███████║
% ██║╚██╔╝██║██╔══██║   ██║   ██╔══██║
% ██║ ╚═╝ ██║██║  ██║   ██║   ██║  ██║
% ╚═╝     ╚═╝╚═╝  ╚═╝   ╚═╝   ╚═╝  ╚═╝
%  →-.   →  .-'.   →  .--.   →  .--.   →  .--.   →  .-→
% →::.\::::::::.\::::::::.\::::::::.\::::::::.\:::::::→→ →
% → →:.\:::: https://github.com/nosvagor/notes \:::→→ →  →  →
% →::::.\::::::::.\::::::::.\::::::::.\::::::::.\::::→  →
%   → →   `--'  →   `.-'  →   `--'  →   `--'  →   `--' →  →

\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{nosvagor-math}

% ╔═╗┌─┐┬─┐┌┬┐┌─┐┌┬┐┌┬┐┬┌┐┌┌─┐
% ╠╣ │ │├┬┘│││├─┤ │  │ │││││ ┬
% ╚  └─┘┴└─┴ ┴┴ ┴ ┴  ┴ ┴┘└┘└─┘
\RequirePackage{
  mathtools, amssymb, amsthm, bm, relsize, esvect,tikz,tikz-cd, array,marvosym,
  etoolbox, mathdots, pgfplots,
}

  \pgfplotsset{compat=newest}

  \numberwithin{equation}{chapter}
  \DeclareMathSizes{12}{14}{10}{8}

  % Augmented Matrix [ccc|c]
  \makeatletter
      \renewcommand*\env@matrix[1][*\c@MaxMatrixCols c]{%
      \hskip -\arraycolsep
      \let\@ifnextchar\new@ifnextchar
      \array{#1}}
  \makeatother

  % bigger epsilon fonts
  \let\origVarEpsilon\varepsilon
  \renewcommand*\varepsilon{\mathlarger{\mathlarger\origVarEpsilon}}
  \def\dvarepsilon{\mathlarger{\varepsilon}} % inline text

  \let\origEpsilon\epsilon
  \renewcommand*\epsilon{\mathlarger{\mathlarger\origEpsilon}}
  \def\depsilon{\mathlarger{\epsilon}} % inline text

% ╔═╗┌─┐┌┬┐┌┬┐┌─┐┌┐┌┌┬┐┌─┐
% ║  │ │││││││├─┤│││ ││└─┐
% ╚═╝└─┘┴ ┴┴ ┴┴ ┴┘└┘─┴┘└─┘

% Inline bold math
\newcommand{\tbm}[1]{\(\bm{#1}\)}

% Large, bold
\newcommand{\chonk}[1]{{\centering\Large{\2{\textbf{#1}}}\par}}

% Alternative fonts
\newcommand{\nil}{\text{\MVZero}~}
\def\i{\mathfrak{i}}

% ihat, jhat, khat
\newcommand*{\ih}{
    \mbox{\fontsize{17.28}{21.6}\selectfont\(\hat{\imath}\)}}
\newcommand*{\jh}{
    \mbox{\fontsize{17.28}{21.6}\selectfont\(\hat{\jmath}\)}}
\newcommand*{\kh}{\hat{k}}

% adding with frequent use
\newcommand{\R}{\mathbb{R}}
\newcommand{\C}{\mathbb{C}}
\newcommand{\N}{\mathbb{N}}
\newcommand{\Q}{\mathbb{Q}}
\newcommand{\Z}{\mathbb{Z}}
\newcommand{\F}{\mathcal{F}}
\newcommand{\I}{\1{\mathcal{I}}}
\def\hopital{L'H\^{o}pital}
\def\P{\mathcal{P}}

% Operator names
\def\med{\operatorname{med}{\hspace{-2pt}}}
\def\cov{\operatorname{cov}{\hspace{-2pt}}}
\def\sgn{\operatorname{sgn}{\hspace{-2pt}}}
\def\var{\operatorname{var}{\hspace{-2pt}}}
\DeclareMathOperator{\arcsec}{arcsec}
\DeclareMathOperator{\arccot}{arccot}
\DeclareMathOperator{\id}{id}
\DeclareMathOperator{\arccsc}{arccsc}
\newcommand{\rref}[1]{\operatorname{rref}\left(#1\right)}
\newcommand{\tr}[1]{\operatorname{tr}\left(#1\right)}
\newcommand{\rank}[1]{\operatorname{rank}\left(#1\right)}
\renewcommand{\det}[1]{\operatorname{det}\left(#1\right)}
\newcommand{\proj}[2]{\operatorname{proj}_{\bm{#1}}\bm{#2}}
\DeclarePairedDelimiterX{\norm}[1]{\lVert}{\rVert}{#1}

\def\given{~\2{\impliedby}~}
\def\then{~\2{\implies}~}
\def\thus{~\2{\therefore}~}
\renewcommand{\qed}{\qquad{\color{primary-1}\blacksquare}}
\newcommand{\tqed}{\hfill{\color{primary-1}\blacksquare}}
\let\oldiff\iff
\renewcommand{\iff}{~\2{\oldiff}~}

\def\t{\dagger}


